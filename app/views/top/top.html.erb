<div class="top_wrapper">
  <div class="top_inner">
    <div class="top_box">
      <h1 class="top_title">
      「日本語ってむずかしい」をゼロに
      </h1>
      <p class="top_about">
      あなたが書いた文章をより良いものになる<br>言葉のレビューサイトです。
      </p>
      <div class="top_btns">
        <% if current_user %>
          <%= link_to(new_post_path) do %>
            <button type="button" name="button" class="top_btn">
              文章を投稿する
            </button>
          <% end %>
        <% else %>
          <%= link_to(login_path) do %>
            <button type="button" name="button" class="top_btn">
              ログインして投稿する
            </button>
          <% end %>
         <% end %>
    </div>
    </div>
    
    
  </div>
</div>
<div class="post_wrapper">
  <div class="post_inner">
    <ul class="index_ul">
      <% @posts.each do |post| %>
        <%= link_to(post_path(post)) do %>
          <li>
            <p class="post_index_tit"><%= post.title %></p>
            <p class="post_index_body"><%= post.body[0, 40] %></p>
          </li>
        <% end %>
      <% end %>
    </ul>
  </div>
</div>
